## 词汇对照表

- 大包：官方 `接入设备` 页面的API接口，默认情况下只有在进入页面瞬间和手动展开/关闭“在线设备、离线设备、黑名单”时才会主动请求的包。该包不仅包含RSSI，还包含整个组网体系里的所有设备MAC，甚至还包括很多和瞬时网速无关的策略之类的，且该拼接容易对ZTE这种强NPU路线的机器的CPU造成较大压力负载。要知道这玩意可是用HTTPS访问后台都有可能会卡的类型。
- 中包：官方 `组网管理` 页面的API接口，这里的组网管理可不是拓扑图。事实上，这里有针对每个设备的名称、MAC、IPv4、IPv6、瞬时上行速率、下行速率（这里所谓的瞬时依然为固定时间更新一次，只是这里通常不是为刷新周期内的平均速度，而是极短采样窗口例如 50/100ms内的速率，且更新频率受限，并不意味着实时更新）。原界面非常方便，只是失去了网速占比、进度条之类的简单 UI 增强功能；该页面默认官方是 5 秒向后台发送一次请求，只可惜在很多新版机型上，组网管理被阉割成了 Mesh 拓扑，导致你只能看到路由器的大头照。<br>
这是该程序的绝对主力，我们的绝大多数统计都是基于中包实现的。它不仅相对而言更省性能（在我家大约 16~24 KiB），也是绝大多数插件和第三方集成作者未曾想到的。只是可惜该设备只包含该AP或网关下的设备，例如主路由不包含下挂子路由的设备，又如子路由只显示当前这台机器的接入。

- 小包：原理上其实并不复杂，本质上相当于官方的接入设备然后点开具体的某台设备，观察它的网速。实质上就是通过复杂的推断算法尽可能在对家庭网络干扰最小的情况下，获得设备的增删，以及从主路漫游道旁路，具体的设备集合有无变化之类的来推断。只有在设备大概率发生变化的时候，才轮询一次大包（当然 RSSI 功能必然会周期性的轮询大包，我已经将其频率设得非常低，但是请求的路由器就要把它给用好不要浪费每一次实际造成的开销，所以我将其也用于了设备变化推断中），然后凡是中包没有囊括的设备，全部通过逐 MAC 小包单独狙击。（以上绝对不是项目工程黑话，我自己就是这么认为的，否则我闲得蛋疼专门去写设备比对算法干什么？我的第一直觉就是，这是“小包”，不信看架构图）。
---
- XHR：我尝试过 XHR 和 Fetch 两种模式，实质上确实只捕获到了 XHR 请求，但是需要注意的是，中兴官方页面的逻辑是你停留在哪里，就给你展示哪里，所以只有你主动打开组网管理，况且新版UI几乎都砍了，才存在这个状态。这就是为什么我们的 WAN 一直都要主动 fetch，因为你肯定是停留在信息量更大的界面，而不是在首页看路由器的大头照。
- 三种主要模式：A模式：官方页面寄生+DOM突变，B1模式：主线、B2：和B1无缝自动切换，主要针对隐藏Mesh等设备逐个发`小包`，C模式：`forceMeshMode===2`时的表现；A到B的切换不可逆；时钟：和B1/2本身是两个不同维度的量，轮询间隔是否相等会改变单/双时钟设定，并对实现细节产生一些改变。
- 事件驱动积分：为什么这么做看正文。
- GroupTime：组时间。为降低低网速下事件驱动误判实际网速撞车为缓存数据，降低巧合概率而进行的设计。
- 其它知识：回流、归零、继承、诈尸：指的是 ZTE 累计设备字节流量计数器的缺陷，为典型系统错误，超出精度类误差的现象；<br>
- 积分：通过事件驱动原理，基于离散采样的梯形数值定积分得到的流量值。
- 高精：对于小流量设备而言，通常非常精确（但是如果产生漫游的话那另说）未必永远高精度，只是对官方数进行了各种处理以后得到的累计值。在高网速下，可能官方计数器都不一定准。
- 测控：测控技术与仪器（Measurement and Control，M&C），是我学弟的专业，他的专业是个需要和某些特定设备打交道的一个软硬件结合的专业。他和我走进网络有很大的相关。
- 内外网比：通过三个数据源和一定的经验数值进行的一个综合对比，并没有固定一种写法数据源；本质是在有限状态和数学公式中，尽量避免用拍脑袋的 if 兜底写法，而用原理本身来表达状态。
- 物理端口：端口级别的流量，同时包含WAN和LAN，而本程序其它提LAN 的地方很多指的是逐 MAC/设备。
- 盲漫游：不是 Wi-Fi 标准术语，是项目对“主路由看不见真实无线归属，终端被识别成有线”等环境的专门叫法。
- 帧：并非屏幕刷新帧率，而是指程序的一个主周期。主周期在绝大多数时候都等于 `WAN` 的采样周期。
- 双重计费：在早期版本尚没有引入 `aR` 的前提情况下，为了防止归零回流，对基线做修改，却导致了漫游后被两次计算。
- 双向奔赴：也就是防抖算法，但依赖于双方的共同作用。
- 保活：为防止路由器 Web后台 将长时间未操作的 Session 踢下线的方法之一，Fetch数据这个动作本身不被某些品牌认为是人类正常的操作，因此使用开页面保活是最简单也最安全的持续登录方式之一。
- 鸭子测试：如果一个事物看起来像鸭子、游泳像鸭子、叫声像鸭子，那么它大概率就是一只鸭子：我们完全没有必要在这里引入数据测试脚本这种预制菜，除非你想当西贝微波炉主理人。只要路由器同一世代、不同手机、相似接入方式，不同的、多台终端网卡或OS统计到的流量值和本插件之间的系数K始终在一定的合理范围内，那就能说明我们的脚本能够尽可能地还原真实世界。

### [点击查看个人主页](https://github.com/ucxn/BroTech)